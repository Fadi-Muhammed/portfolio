/**
 * Stands in for the `server-only` package under vitest. See vitest.config.mts for why.
 *
 * Deliberately empty: importing it must do nothing, which is the opposite of what the
 * real package does and the entire point of the substitution.
 */
export {};
